# Set i2c interface faster. Without this, the 200samplesper sec of the IMU overloads the interface
# /boot/config.txt

dtparam=i2c_arm=on,i2c_arm_baudrate=400000

# Enable VNC and SSH (from gui)

# Enable display blanking: 
# Preferences -> Raspberry Pi Configuration -> Display -> Screen Blanking

# Add CPU % and Temp to toolbar

# Add Geany to toolbar

